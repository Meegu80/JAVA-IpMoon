// 이 클래스는 'exercise_Q1'이라는 이름으로 정의되었습니다.
// public 접근 제어자를 사용하여 어디서든 접근 가능하도록 설정되었습니다.
public class exercise_Q1 {
    // main 메소드는 Java 프로그램의 진입점입니다.
    // public static void main(String[] args)는 Java의 표준 main 메소드 선언입니다.
    // public: 어디서든 접근 가능
    // static: 객체 생성 없이 호출 가능
    // void: 반환값이 없음
    // String[] args: 명령줄 인자를 받기 위한 매개변수
    public static void main(String[] args) {
        // 정수형 변수 myAge를 선언하고 23으로 초기화합니다.
        // int는 32비트 정수를 저장할 수 있는 기본 데이터 타입입니다.
        int myAge = 23;
        
        // 정수형 변수 teacherAge를 선언하고 38로 초기화합니다.
        int teacherAge = 38;

        // boolean 변수 value를 선언하고, myAge가 25보다 큰지 비교한 결과를 저장합니다.
        // boolean은 true 또는 false 값만을 가질 수 있는 논리 데이터 타입입니다.
        // 여기서 myAge(23)는 25보다 작으므로, value에는 false가 저장됩니다.
        boolean value = (myAge > 25);
        
        // value 변수의 값을 콘솔에 출력합니다. 결과는 false입니다.
        System.out.println(value);

        // myAge가 25 이하인지 비교한 결과를 직접 콘솔에 출력합니다.
        // 23은 25 이하이므로, 결과는 true입니다.
        System.out.println(myAge <= 25);
        
        // myAge와 teacherAge가 같은지 비교한 결과를 콘솔에 출력합니다.
        // 23과 38은 다르므로, 결과는 false입니다.
        System.out.println(myAge == teacherAge);

        // char 타입의 변수 ch를 선언합니다.
        // char는 유니코드 문자 하나를 저장할 수 있는 16비트 기본 데이터 타입입니다.
        char ch;
        
        // 삼항 연산자를 사용하여 ch에 값을 할당합니다.
        // (조건) ? 참일 때의 값 : 거짓일 때의 값
        // myAge > teacherAge 조건이 거짓이므로, ch에는 'F'가 할당됩니다.
        ch = (myAge > teacherAge) ? 'T' :'F';
        
        // ch 변수의 값을 콘솔에 출력합니다. 결과는 'F'입니다.
        System.out.println(ch);
    }
}
